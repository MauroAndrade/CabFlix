@model List<Cabflix.Models.Database.UnidadeDeMedida>

@{
    ViewBag.Title = "Index";
}



<section class="content margin-top-10">

    <div class="row">
        <div class="col-md-12">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Unidade de Medida</h3>
                    <div class="card-tools">
                        <a href="@Url.Action("Create", "UnidadeMedida")" class="btn btn-tool" data-toggle="tooltip" title="Adicionar Unidade de Medida"><i class="fas fa-plus"></i></a>
                    </div>
                </div>
                <div class="card-body" style="display: block;">

                    <table id="usuario" class="table table-bordered table-striped table-row">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Descrição</th>
                                <th>Símbolo</th>
                                <th>Ação</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr id="@item.Id">
                                    <td>@item.Nome</td>
                                    <td>@item.Descricao</td>
                                    <td>@item.Simbolo</td>
                                    <td class="btn-group btn-group-sm">
                                        <a href="@Url.Action("Edit", "UnidadeMedida", new { id = item.Id})" class="btn btn-info"><i class="fas fa-pencil-alt"></i></a>
                                        <a href="@Url.Action("Delete", "UnidadeMedida", new { id = item.Id})" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                    </td>
                                </tr>
                            }


                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Nome</th>
                                <th>Descrição</th>
                                <th>Símbolo</th>
                                <th>Ação</th>
                            </tr>
                        </tfoot>
                    </table>



                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>

    </div>


</section>


