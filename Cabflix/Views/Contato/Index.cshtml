@model List<Cabflix.Models.Database.Contato>

@{
    ViewBag.Title = "Index";
}



<section class="content margin-top-10">

    <div class="row">
        <div class="col-md-12">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Contato</h3>
                    <div class="card-tools">
                        <a href="@Url.Action("Create", "Contato")" class="btn btn-tool" data-toggle="tooltip" title="Adicionar Contato"><i class="fas fa-plus"></i></a>
                    </div>
                </div>
                <div class="card-body" style="display: block;">

                    <table id="usuario" class="table table-bordered table-striped table-row">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>Email</th>
                                <th>Cargo</th>
                                <th>Responsável</th>
                                <th>Ação</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr id="@item.Id">
                                    <td>@item.Nome</td>
                                    <td>@item.Email</td>
                                    <td>@item.Cargo</td>
                                    <td>@item.FkResponsavel</td>
                                    <td class="btn-group btn-group-sm">
                                        <a href="@Url.Action("Edit", "Contato", new { id = item.Id})" class="btn btn-info"><i class="fas fa-pencil-alt"></i></a>
                                        <a href="@Url.Action("Delete", "Contato", new { id = item.Id})" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                                    </td>
                                </tr>
                            }


                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Nome</th>
                                <th>Email</th>
                                <th>Cargo</th>
                                <th>Responsável</th>
                                <th>Ação</th>
                            </tr>
                        </tfoot>
                    </table>



                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>

    </div>


</section>


