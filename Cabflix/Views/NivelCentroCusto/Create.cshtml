@model Cabflix.Models.Database.CcNivel

@{
    ViewBag.Title = "Create";
}


<section class="content margin-top-10">

    @using (Html.BeginForm("Create", "NivelCentroCusto", FormMethod.Post, new { @class = "col s12" }))
    {
        <div class="row">
            <div class="col-md-12">
                <div class="card card-primary">
                    <div class="card-header">
                        <h3 class="card-title">Cadastrar Nível</h3>

                        @*<div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                                    <i class="fas fa-minus"></i>
                                </button>
                            </div>*@
                    </div>
                    <div class="card-body" style="display: block;">


                        @Html.AntiForgeryToken()

                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    @Html.LabelFor(Model => Model.Nome)
                                    @Html.EditorFor(Model => Model.Nome, "", new { htmlAttributes = new { @class = "form-control validate" } })
                                    @Html.ValidationMessageFor(Model => Model.Nome)
                                </div>
                                @*<div class="form-group">
                                    @Html.LabelFor(Model => Model.Indice)
                                    @Html.EditorFor(Model => Model.Indice, "", new { htmlAttributes = new { @class = "form-control validate" } })
                                    @Html.ValidationMessageFor(Model => Model.Indice)
                                </div>*@
                                

                            </div>

                        </div>


                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>

        </div>
        <div class="row">
            <div class="col-12">
                <a href="@Url.Action("Index", "NivelCentroCusto")" class="btn btn-secondary">Cancelar</a>
                <input type="submit" value="Salvar" class="btn btn-success float-right">
            </div>
        </div>
    }
</section>

